---

- hosts: localhost
  remote_user: root
  become: yes
  vars:
    nginx_confd_files: 
      - ./conf.d/http_core.conf
      - ./conf.d/http_gzip.conf
      - ./conf.d/http_log.conf
      - ./conf.d/http_proxy.conf
    nginx_sites_available_files:
      - ./tests/files/sites-available/test.conf
    nginx_serverblock_files_removed: []
  
  pre_tasks:
    - name: Update apt cache.
      apt: 
        update_cache: yes 
        cache_valid_time: 600
      when: ansible_os_family == 'Debian'
      changed_when: false

    - name: Install dependencies.
      package: 
        name: curl
        state: present
  
  # tasks:
  #   - name: List contents of sites-available
  #     shell: ls /etc/nginx/sites-enabled
  #     changed_when: false
  #     register: command_result

  #   - debug:
  #       msg: "sites-enabled: {{ command_result }}"

    # - name: Read the serverblock file
    #   shell: cat /etc/nginx/sites-enabled/test.conf
    #   changed_when: false
    #   register: test_serverblock

    # - debug:
    #     msg: "Test serverblock: {{ test_serverblock }}"

    # - name: Read the nginx.conf file
    #   shell: cat /etc/nginx/nginx.conf
    #   changed_when: false
    #   register: test_nginxconf

  #   - debug:
  #       msg: "Test nginx.conf: {{ test_nginxconf }}"

  #   - name: Read the http_core.conf file
  #     shell: cat /etc/nginx/conf.d/http_core.conf
  #     changed_when: false
  #     register: test_http_core

  #   - debug:
  #       msg: "Test http_core.conf: {{ test_http_core }}"

  roles:
    - role_under_test
